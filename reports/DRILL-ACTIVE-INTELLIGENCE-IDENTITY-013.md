# DRILL — ACTIVE INTELLIGENCE IDENTITY — 013

**Date:** 2026-09-10
**Scope:** GLOBAL / METAINTELIGENCIA
**Mode:** READ-ONLY / EVIDENCE-BOUND

## Question

Can the active intelligence references be selected with a deterministic minimum predicate and then serialized reproducibly, without creating a new authority?

## Evidence

`atlas-cos-v1/protocols/EVO-v1.md` remains the canonical evolutionary source. It states that the nucleus does not change, knowledge is governed, every evolution follows the same cycle, and the evolution reaches a new baseline only through evidence and certification. fileciteturn151file0L2-L2

The Metaintelligence domain already defines Intelligence Version as the state of COS intelligence rather than a software release, and explicitly prohibits version advancement from conversation, proposal, activity, or documentation alone. fileciteturn150file0L2-L2

The physical KBP state currently demonstrates `knowledge_version=KBP-v1`, `cos_version=3.5`, `integrity=100`, and `status=ready`, while `manifest_hash` is NULL. This proves a governed knowledge state but not a transversal intelligence composition.

## Minimum selection predicate

For each reference class, active identity requires only a current governed reference that is authorized for the declared scope and is not superseded.

```text
CURRENT + AUTHORIZED + NOT SUPERSEDED
                ↓
       ACTIVE REFERENCE
```

Runtime materialization and execution evidence remain outside identity.

## Deterministic composition

The minimum manifest candidate is therefore a fixed ordered set of references:

```text
scope
cos_ref
knowledge_ref
capability_ref
integrity_ref
supersession_ref
baseline_ref
```

The references must be canonical identifiers, not copied state blobs. The same ordered input must produce the same serialized representation and therefore the same hash.

```text
ACTIVE REFERENCES
      ↓
FIXED ORDER
      ↓
CANONICAL SERIALIZATION
      ↓
HASH
      ↓
INTELLIGENCE VERSION ID
```

## Result

**TRANSFORM — CONFIRMED.**

The missing mechanism is not a new state system, authority, runtime, or QA layer. It is the deterministic composition function that converts already-governed active references into one reproducible intelligence identity.

## Boundary

```text
IDENTITY
  what is active

OPERABILITY
  can it operate

EVIDENCE
  was behavior demonstrated
```

No runtime failure may silently rewrite the intelligence identity.

## Next frontier

Determine the canonical identifier for each of the six reference classes and test one complete serialization/hash without introducing a new table or authority.

**OVR global:** 45% — unchanged.
**V Edad research bar:** 40% — unchanged.
